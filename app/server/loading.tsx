import { PageSkeleton } from "@/components/skeletons";

/**
 * Server page loading UI
 * Displayed while the server example page is loading
 */
export default function Loading() {
	return <PageSkeleton />;
}
