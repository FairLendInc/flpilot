import { ListingsGridSkeleton } from "@/components/skeletons";

/**
 * Listings page loading UI
 * Displayed while the listings grid is loading
 */
export default function Loading() {
	return <ListingsGridSkeleton />;
}
